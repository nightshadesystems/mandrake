# branding

Loader banner, MOTD, `/etc/release`, default prompt, and console title. All
say Mandrake / Nightshade Systems. OmniOS CE stays credited in `/etc/release`;
no upstream attribution or copyright file is removed (spec §11).

Populated in Phase 1.
