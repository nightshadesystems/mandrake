# Product and project names that clippy::doc_markdown would otherwise flag as
# unticked identifiers. ".." keeps clippy's built-in list.
doc-valid-idents = ["OmniOS", "SmartOS", "OpenAPI", "WebSocket", "SQLite", "Crossbow", ".."]
